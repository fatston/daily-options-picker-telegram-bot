{
  "name": "daily-options-picker-online-bot",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node --check api/telegram.js && node --check api/publish.js && node --check api/picks.csv.js && node --check api/health.js && node --check lib/bot.js && node --check lib/brief-store.js && node --check lib/pick-store.js && node --check lib/telegram.js && node --check scripts/seed-picks-csv.js",
    "test": "node tests/run-tests.js",
    "seed:picks": "node scripts/seed-picks-csv.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/blob": "^2.4.0"
  }
}
